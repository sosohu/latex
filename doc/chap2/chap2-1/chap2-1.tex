\ifx allfiles undefined
\documentclass{article}
\usepackage{CJK}

%%%代码
\usepackage{color}
\usepackage{xcolor}
\definecolor{keywordcolor}{rgb}{0.8,0.1,0.5}
\usepackage{listings}
\lstset{breaklines}%这条命令可以让LaTeX自动将长的代码行换行排版
\lstset{extendedchars=false}%这一条命令可以解决代码跨页时，章节标题，页眉等汉字不显示的问题
\lstset{language=C++, %用于设置语言为C++
	keywordstyle=\color{keywordcolor} \bfseries, %设置关键词
	identifierstyle=,
	basicstyle=\ttfamily, 
	commentstyle=\color{blue} \textit,
	stringstyle=\ttfamily, 
	showstringspaces=false,
	%frame=shadowbox, %边框
	captionpos=b
}
%%%

%\hypersetup{CJKbookmarks=true} %解决section不能使用中文的问题

\begin{document}
\begin{CJK}{UTF8}{gbsn}     %CJK:支持中文

\else
	
%二叉树的遍历
\qquad二叉树的遍历是解决很多二叉树问题的基础，它的递归写法和非递归写法更是需要都要掌握的，这里的遍历就是将树的节点都依次访问一遍，因为访问顺序的问题，就可以分为前序，中序，后序以及层次等很多种遍历的方法。
\subsection{PreOrederTraversal}
\input{chap2/chap2-1/PreOrederTraversal.tex}
\subsection{InOrederTraversal}
\input{chap2/chap2-1/InOrederTraversal.tex}
\subsection{PostOrederTraversal}
\input{chap2/chap2-1/PostOrederTraversal.tex}
\subsection{LevelOrderTraversal}
\input{chap2/chap2-1/LevelOrederTraversal.tex}
\subsection{Recover Binary Search Tree }
\input{chap2/chap2-1/Recover_Binary_Search_Tree.tex}

\fi

\ifx allfiles undefined
\end{CJK}
\end{document}
\fi
